spring:
  application:
    name: cafes

  datasource:
    url: ${MYSQL_DATABASE_URL}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}

server:
  port: ${BACKEND_CONTAINER_PORT}
  servlet:
    context-path: /api/v1
